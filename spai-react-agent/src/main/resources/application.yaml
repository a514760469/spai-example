spring:
    ai:
        dashscope:
            api-key: ${AI_DASHSCOPE_API_KEY}
    application:
        name: react-agent
logging:
    level:
        root: info
        org.springframework.ai.chat.client.advisor: DEBUG